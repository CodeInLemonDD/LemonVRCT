# VRChat Voice Translation Software Dependencies
# Compatible with lower CUDA versions (CUDA 10.2+)

# Core PyTorch with CUDA 11.7 support (compatible with CUDA 10.2+)
torch==2.5.1+cu121
torchvision==0.20.1+cu121
torchaudio==2.5.1+cu121
--index-url https://download.pytorch.org/whl/cu117

# Core dependencies
numpy>=1.24.0

# Speech transcription
openai-whisper>=20231117

# Audio recording
pyaudio>=0.2.11

# Global hotkey listening
pynput>=1.7.6

# OSC communication
python-osc>=1.8.2

# OpenAI API client
openai>=1.3.0

# Environment variable management
python-dotenv>=1.0.0

# GUI interface
ttkbootstrap>=1.10.1

# Note: The following libraries are Python standard libraries and do not need to be installed:
# - os, sys, io, time, threading, queue, json, re, logging, subprocess
# - asyncio, argparse, tkinter (Python standard library)
